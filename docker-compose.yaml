version: "3.9"
services:
  front:
    image: sermalenk/skypro-front:base
    ports:
      - "8080:80"
    depends_on:
      - api

  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./todolist/.env
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "python manage.py makemigrations core &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"

  postgres:
        image: postgres
        env_file:
          - ./todolist/.env
        healthcheck:
          test: [ "CMD-SHELL", "pg_isready -U postgres -d postgres_2" ]
          interval: 5s
          timeout: 5s
          retries: 5
        volumes:
                - ./db/:/var/lib/postgresql/data
