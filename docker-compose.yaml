version: '3.2'
services:
   postgres:
        image: postgres
        restart: always
        environment:
                - POSTGRES_USER=test2
                - POSTGRES_PASSWORD=snowback
        ports:
                - '5432:5432'
        volumes:
                - ./db/:/var/lib/postgresql/data